# Runtime dependencies of the Multimodal ETL pipeline, installed into the Airflow image.
#
# Deliberately without version numbers: the versions are imposed by Airflow's
# official constraints file (see the Dockerfile). Without it, pip would replace
# libraries Airflow itself depends on — SQLAlchemy in particular — and the
# scheduler would no longer start.
#
# Streamlit and the development tools are not needed on the Airflow side.
requests
feedparser
beautifulsoup4
lxml
tldextract
python-dotenv
pandas
pyarrow
pillow
sqlalchemy
